// React hook = 
              // it is special function that allows functional components to use React features without writing class components (React v16.8)
              // (useState, useEffect, useContext, useReducer, useRef, useMemo, useCallback, useLayoutEffect, useDebugValue and more)


// useState() =
          // it is a React hook that allows the creation of stateful variables AND a setter function to update its value in the virtual dom.
          // [name, setName]

function App(){


  return(
    <>
    
    </>
  );
}

export default App
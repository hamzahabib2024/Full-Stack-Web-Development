// click events:
//          // it is an interection when a user clicks on a specific element on the page. 
//              In React, we can handle click events by using the onClick event handler. 
//          //This allows us to define a function that will be executed when the user clicks on the element.

// we can respond to the clicks by passing a callback to the onClick event handler.


import Button from './Button.jsx'

function App() {

  return (
    <>
      <Button />
    </>
  );
}

export default App
